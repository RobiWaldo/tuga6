{
  "name": "tugas6robi",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run json-server\" \"vite\"",
    "build": "vite build",
    "preview": "vite preview",
    "json-server": "json-server --watch db.json --port 3000",
    "start:api": "json-server --watch db.json --port 3000",
    "start": "npm run start:api & vue-cli-service serve"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "bootstrap": "^5.3.3",
    "json-server": "^1.0.0-beta.0",
    "sweetalert2": "^11.11.0",
    "vue": "^3.4.21"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^5.2.0"
  }
}
